const mongoose = require('mongoose');

const userSchema = new mongoose.Schema({
  email: {
    type: String,
    required: true,
    unique: true,
  },
  password: {
    type: String,
    required: true,
  },
});

const Editor = mongoose.model('Editor', userSchema, "editor_login");

module.exports = Editor;
